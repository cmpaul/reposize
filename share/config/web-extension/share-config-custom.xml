<alfresco-config>
	<!-- Put Surf into development mode - do not use in production! -->
	<!-- 
	<config evaluator="string-compare" condition="WebFramework">
		<web-framework>
			<autowire>
				<mode>development</mode>
			</autowire>
		</web-framework>
	</config>
	-->

	<config replace="true">
		<flags>
			Developer debugging setting to turn on DEBUG mode for client scripts in the browser - do not use in production!
			<client-debug>true</client-debug>
			LOGGING can always be toggled at runtime when in DEBUG mode (Ctrl, Ctrl, Shift, Shift). This flag automatically 
				activates logging on page load. - do not use in production!
			<client-debug-autologging>false</client-debug-autologging>
		</flags>
	</config>
</alfresco-config>